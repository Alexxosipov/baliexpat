<header class="text-gray-100 body-font">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex flex-wrap py-5 flex-col md:flex-row items-center">
        <a class="flex title-font font-semibold uppercase items-center mb-4 md:mb-0" href="{{ route('index') }}">
            <img src="/images/logo.png" alt="" width="50">
        </a>
        <nav class="md:ml-auto flex flex-wrap space-x-6 text-base justify-center">
            <a class="uppercase font-bold text-sm border-b-2 border-transparent hover:border-gray-50 hover:text-gray-50" href="{{ route('squad.index') }}">Squad</a>
            <a class="uppercase font-bold text-sm border-b-2 border-transparent hover:border-gray-50 hover:text-gray-50" href="{{ route('games.index') }}">Games</a>
            <a class="uppercase font-bold text-sm border-b-2 border-transparent hover:border-gray-50 hover:text-gray-50" href="#">Contact us</a>
        </nav>
    </div>
</header>
